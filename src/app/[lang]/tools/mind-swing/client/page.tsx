import { ClientView } from "./ClientView";

const MindSwingClientPage = async () => {
  // const t = await getTranslations("ToolsPage");

  return <ClientView />;
};

export default MindSwingClientPage;
